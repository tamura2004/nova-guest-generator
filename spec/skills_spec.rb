require "skills"

describe Skills do

	# subject(:skills) do

	# 	Skills.new(["カブキ","アヤカシ","カブト"])
	# end

	# context :SKILLS do
	# 	it { expect(Skills::SKILLS).to all(be_a Skill) }
	# end

	# context :my_skills do
	# 	it { expect(skills.my_skills(1)).to all(be_a Skill) }
	# end

	# context :initialize do
	# 	it { expect(skills).to all(be_a Skill) }
	# 	it { expect(skills.basic).to all(be_a Skill) }
	# 	it { expect(skills.not_basic).to all(be_a Skill) }
	# end
end
